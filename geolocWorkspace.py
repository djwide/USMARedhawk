constants gpspair 1,2,3

def pdoa(self):
	#receive message
	#unpack data
	#check timestamp
	#create 3 queues to hold data
	#(pwoer and gps)
	for x <- xrange:
		for y <- yrange:
			#will have timestamp data
			d1= distance from circle 1
			d2= distance from circle 2
			d3= distance from circle 3
			array(x)(y)= intensity(d1)*intensity(d2)*intensity(d3)
